node_modules/
.next/
out/
dist/
build/
.env
.env.local
.env.production
.DS_Store
*.zip

# Admin panel: local SQLite database + uploaded project photos/videos.
# These are runtime data, not source — don't commit them, and definitely
# back them up separately (they're not stored anywhere else).
/data/
/storage/
